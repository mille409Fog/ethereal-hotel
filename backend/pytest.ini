[pytest]
# Only collect the real automated suite. The legacy manual smoke script
# (test_api.py) hits a live server and is not part of the pytest run.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
