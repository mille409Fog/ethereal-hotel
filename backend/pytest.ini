[pytest]
# The whole suite lives in tests/ and needs no running server: conftest.py
# builds an isolated, deterministically seeded SQLite database per session.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
