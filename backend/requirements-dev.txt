# Development and CI dependencies: the test suite plus the static analysis
# gates. Install with:
#
#     pip install -r requirements-dev.txt
#
# Pinned exactly for the same reason the runtime deps are: a linter that
# silently gains a rule on a Tuesday turns a green branch red with no commit
# behind it. Dependabot proposes the upgrades as reviewable PRs instead.

-r requirements.txt

# Tests
pytest==9.1.1          # 8.3.3 carries PYSEC-2026-1845; 9.0.3 is the fix floor
httpx==0.28.1          # required by fastapi.testclient.TestClient

# Static analysis — see pyproject.toml at the repo root for the configuration.
ruff==0.16.2
mypy==2.3.0

# Advisory scanner for both lists above, run in CI by
# .github/workflows/supply-chain.yml and locally by `npm run audit:py`.
#
# It is declared here, in the file it scans, which means it scans itself. That
# is deliberate: this repo already treats an advisory against pytest or ruff as
# a real failure — nothing in the gate distinguishes tooling from runtime — so
# exempting the scanner would be the one inconsistency worth objecting to.
pip-audit==2.10.1
